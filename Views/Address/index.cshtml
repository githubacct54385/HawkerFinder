@model IEnumerable<HawkerFinder.Models.Address>

@{
    ViewData["Title"] = "Index";
}

<h2>All Hawker Centres</h2>

<form action="/Address/CloseHawkers" method="POST">
  <fieldset>
      <legend>Find closest Hawkers to a coordinate</legend>
      <div id="rdo_btns">
        <label>By Location
            <input type="radio" checked="checked" name="radio" id="byLocationRdo">
        </label>
        <label>By Coordinates
            <input type="radio" name="radio" id="byCoordsRdo">
        </label>
      </div>
      <div>
          <label for="Latitude">Latitude</label>
          <input type="number" name="givenLatitude" step="0.01" placeholder=1.30 />
      </div>
      <div>
          <label for="Longitude">Longitude</label>
          <input type="number" name="givenLongitude" step="0.01" placeholder=103.00 />
      </div>
      <div>
          <label>&nbsp;</label>
          <input type="submit" value="Submit" class="submit" />
      </div>
  </fieldset>
</form>

<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Addr)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.latitude)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.longitude)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Addr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.latitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.longitude)
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    $(document).ready(function(){
        alert("Hello World!");
        /*$('#rdo_btns').click(function() {
            if($('#byLocationRdo').is(':checked')) { 
                alert("it's checked"); 
            }
            else if($('#byCoordsRdo').is(':checked')) { 
                alert("it's checked"); 
            }
        });*/

        $(function(){
            $('input[type="radio"]').click(function(){
                if ($(this).is(':checked')) {
                    alert($(this).val());
                }
            });
        });
    }
</script>